#ifndef MERGE_SORT_H
#define MERGE_SORT_H

int* merge(int*, int*, int*, const unsigned int, const unsigned int);
int* mergeSortAlgorithm(int*, int*, unsigned int, int);
void* parallelMergeSort(void*);
int* mergeSort(int*, int);
void parallelMergeSortAlgorithm(int, int*, int);

#endif // MERGE_SORT_H